export function isEmailList(emails: string): boolean;

/**
 * Takes input 'text', splits it on colons/semicolons, trims whitespaces. Returns result.
 * @param text
 */
export function splitTextOnCommasAndSemicolons(text: string): string[]
